/* jshint expr: true */
'use strict';

require('../../app/scripts/components/Premise');

describe('Premise', function () {
  describe('give it some context', function () {
    it('should assert something', function () {
      expect(true).to.be.true;
    });
  });
});
